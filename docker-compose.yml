services:
  security-monitor:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - bot/.env
    ports:
      - "7879:7879"
    volumes:
      - security-monitor-data:/app/data

volumes:
  security-monitor-data:
